# inverteer

Minimal `caliss` page with `name`, `number`, and `mail` fields in `index.html`.

Open the repository's local `index.html` file in a browser to view the form.
Submitting the form requires a backend that accepts `POST /contact`.
